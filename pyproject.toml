[project]
name = "ll-router"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "httpx>=0.28",
    "pyyaml>=6",
]

[project.scripts]
ll-router = "ll_router.main:run"
